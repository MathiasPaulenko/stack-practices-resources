name: test

on:
  push:
    paths:
      - "resources/patterns/data/batch-to-streaming-bridge-pattern/**"
  pull_request:
    paths:
      - "resources/patterns/data/batch-to-streaming-bridge-pattern/**"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: resources/patterns/data/batch-to-streaming-bridge-pattern
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pytest
      - run: node --test bridge.test.js
      - run: python -m pytest test_bridge.py -v
