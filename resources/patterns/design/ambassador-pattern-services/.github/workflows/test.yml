name: test

on:
  push:
    paths:
      - "resources/patterns/design/ambassador-pattern-services/**"
  pull_request:
    paths:
      - "resources/patterns/design/ambassador-pattern-services/**"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: resources/patterns/design/ambassador-pattern-services
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pytest
      - run: node --test ambassador.test.js
      - run: python -m pytest test_ambassador.py -v
